extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/product-layout.pug

block main
    .container.my-3 
        .row 
            .col-12 
                +box-head(pageTitle) 
    
        if(products.length)
            +product-grid(products)
        else 
            p(class="text-center") Không có sản phẩm