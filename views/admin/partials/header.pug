header.header 
    .container-fluid 
        .row.align-items-center 
            .col-3 
                a(href=`${prefixAdmin}/dashboard` class="admin") ADMIN

            .col-9
                div(class="text-right")
                    a(
                        href=`${prefixAdmin}/auth/logout`
                        class="btn btn-danger btn-sm"
                    ) Đăng xuất